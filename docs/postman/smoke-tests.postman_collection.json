{
  "info": {
    "name": "Smoke Tests - Inventory System API",
    "description": "Colección de smoke tests para validación rápida de endpoints críticos del sistema. Ejecutar después de despliegues o cambios importantes.",
    "version": "1.0.0",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Health Check",
      "item": [
        {
          "name": "Basic Health Check",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Health check returns 200', function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "pm.test('Response time < 1s', function () {",
                  "    pm.expect(pm.response.responseTime).to.be.below(1000);",
                  "});",
                  "",
                  "pm.test('Response has status field', function () {",
                  "    const jsonData = pm.response.json();",
                  "    pm.expect(jsonData).to.have.property('status');",
                  "});"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{baseUrl}}/health",
              "host": ["{{baseUrl}}"],
              "path": ["health"]
            },
            "description": "Verifica que el servicio esté disponible y respondiendo"
          }
        }
      ]
    },
    {
      "name": "Authentication",
      "item": [
        {
          "name": "Login",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Login returns 200 or 201', function () {",
                  "    pm.expect([200, 201]).to.include(pm.response.code);",
                  "});",
                  "",
                  "if (pm.response.code === 200 || pm.response.code === 201) {",
                  "    const jsonData = pm.response.json();",
                  "    const data = jsonData.data || {};",
                  "    ",
                  "    pm.test('Response has success and data', function () {",
                  "        pm.expect(jsonData).to.have.property('success');",
                  "        pm.expect(jsonData).to.have.property('data');",
                  "    });",
                  "    ",
                  "    pm.test('Response has accessToken', function () {",
                  "        pm.expect(data).to.have.property('accessToken');",
                  "        pm.collectionVariables.set('accessToken', data.accessToken);",
                  "    });",
                  "    ",
                  "    pm.test('Response has refreshToken', function () {",
                  "        pm.expect(data).to.have.property('refreshToken');",
                  "        pm.collectionVariables.set('refreshToken', data.refreshToken);",
                  "    });",
                  "    ",
                  "    pm.test('Response has user data', function () {",
                  "        pm.expect(data).to.have.property('user');",
                  "    });",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "X-Organization-ID",
                "value": "{{organizationId}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"admin@example.com\",\n  \"password\": \"admin123\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/auth/login",
              "host": ["{{baseUrl}}"],
              "path": ["auth", "login"]
            },
            "description": "Autenticación básica. Actualiza email y password según tu entorno."
          }
        },
        {
          "name": "Refresh Token",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Refresh returns 200', function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "if (pm.response.code === 200) {",
                  "    const jsonData = pm.response.json();",
                  "    const data = jsonData.data || {};",
                  "    ",
                  "    pm.test('Response has success and data', function () {",
                  "        pm.expect(jsonData).to.have.property('success');",
                  "        pm.expect(jsonData).to.have.property('data');",
                  "    });",
                  "    ",
                  "    pm.test('Response has new accessToken', function () {",
                  "        pm.expect(data).to.have.property('accessToken');",
                  "        pm.collectionVariables.set('accessToken', data.accessToken);",
                  "    });",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "X-Organization-ID",
                "value": "{{organizationId}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"refreshToken\": \"{{refreshToken}}\"\n}"
            },
            "url": {
              "raw": "{{baseUrl}}/auth/refresh",
              "host": ["{{baseUrl}}"],
              "path": ["auth", "refresh"]
            },
            "description": "Renovar token de acceso. Requiere refreshToken válido."
          }
        }
      ]
    },
    {
      "name": "Products",
      "item": [
        {
          "name": "Get Products",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Get products returns 200', function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "if (pm.response.code === 200) {",
                  "    const jsonData = pm.response.json();",
                  "    ",
                  "    pm.test('Response has success and data', function () {",
                  "        pm.expect(jsonData).to.have.property('success');",
                  "        pm.expect(jsonData).to.have.property('data');",
                  "    });",
                  "    ",
                  "    pm.test('Response has data array', function () {",
                  "        pm.expect(jsonData.data).to.be.an('array');",
                  "    });",
                  "    ",
                  "    pm.test('Response time < 2s', function () {",
                  "        pm.expect(pm.response.responseTime).to.be.below(2000);",
                  "    });",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}"
              },
              {
                "key": "X-Organization-ID",
                "value": "{{organizationId}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/products?page=1&limit=10",
              "host": ["{{baseUrl}}"],
              "path": ["products"],
              "query": [
                {
                  "key": "page",
                  "value": "1"
                },
                {
                  "key": "limit",
                  "value": "10"
                }
              ]
            },
            "description": "Obtener lista de productos. Requiere autenticación."
          }
        }
      ]
    },
    {
      "name": "Warehouses",
      "item": [
        {
          "name": "Get Warehouses",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Get warehouses returns 200', function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "if (pm.response.code === 200) {",
                  "    const jsonData = pm.response.json();",
                  "    ",
                  "    pm.test('Response has success and data', function () {",
                  "        pm.expect(jsonData).to.have.property('success');",
                  "        pm.expect(jsonData).to.have.property('data');",
                  "    });",
                  "    ",
                  "    pm.test('Response has data array', function () {",
                  "        pm.expect(jsonData.data).to.be.an('array');",
                  "    });",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}"
              },
              {
                "key": "X-Organization-ID",
                "value": "{{organizationId}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/warehouses",
              "host": ["{{baseUrl}}"],
              "path": ["warehouses"]
            },
            "description": "Obtener lista de bodegas. Requiere autenticación."
          }
        }
      ]
    },
    {
      "name": "Reports",
      "item": [
        {
          "name": "Get Available Inventory Report",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('Report returns 200', function () {",
                  "    pm.response.to.have.status(200);",
                  "});",
                  "",
                  "if (pm.response.code === 200) {",
                  "    const jsonData = pm.response.json();",
                  "    ",
                  "    pm.test('Response has success and data', function () {",
                  "        pm.expect(jsonData).to.have.property('success');",
                  "        pm.expect(jsonData).to.have.property('data');",
                  "    });",
                  "    ",
                  "    pm.test('Response has report data', function () {",
                  "        pm.expect(jsonData.data).to.be.an('object');",
                  "    });",
                  "    ",
                  "    pm.test('Response time < 5s', function () {",
                  "        pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "    });",
                  "}"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{accessToken}}"
              },
              {
                "key": "X-Organization-ID",
                "value": "{{organizationId}}"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/reports/inventory/available/view",
              "host": ["{{baseUrl}}"],
              "path": ["reports", "inventory", "available", "view"]
            },
            "description": "Obtener reporte de inventario disponible. Requiere autenticación."
          }
        }
      ]
    }
  ],
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "// Smoke test pre-request script",
          "console.log('🧪 Running smoke test:', pm.info.name);"
        ]
      }
    },
    {
      "listen": "test",
      "script": {
        "type": "text/javascript",
        "exec": [
          "// Global smoke test validations",
          "pm.test('Response time is acceptable', function () {",
          "    pm.expect(pm.response.responseTime).to.be.below(10000); // 10s max",
          "});"
        ]
      }
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:3000",
      "type": "string"
    },
    {
      "key": "organizationId",
      "value": "dev-org",
      "type": "string"
    },
    {
      "key": "accessToken",
      "value": "",
      "type": "string"
    },
    {
      "key": "refreshToken",
      "value": "",
      "type": "string"
    }
  ]
}
